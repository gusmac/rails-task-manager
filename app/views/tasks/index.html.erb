<h1>All Tasks</h1>

<ul>
  <% @tasks.each do |t| %>
    <li><input type="checkbox" value=""><%= link_to t.title, task_path(t) %></li>
  <% end %>
</ul>


<p><%= link_to "Add a new task", new_path %></p>
